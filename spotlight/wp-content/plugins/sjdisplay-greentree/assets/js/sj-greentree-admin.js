(function ($, window, document) {

    //dom ready
    $(function () {

        //order sync admin error notification popup
        if (order_admin_sync_error == 1) {
            alert("Order Sync Error! An email with details has been sent to sales@sjdg.global");
        }

    });

}(window.jQuery, window, document));